export function MarketingFooter() {
  return (
    <footer className="border-t border-border/60 bg-card py-8 text-center text-sm text-muted-foreground">
      © {new Date().getFullYear()} ClientFlow Tax — Tous droits réservés
    </footer>
  );
}
